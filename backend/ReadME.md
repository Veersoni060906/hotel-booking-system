Hotel Booking System - Backend
-----------------------------

Setup:
1. Copy .env and set MONGO_URI
2. npm install
3. npm run seed   # populate DB from data/*.json
4. npm run dev    # start server with nodemon

APIs:
- /api/hotels
- /api/rooms
- /api/guests
- /api/bookings
